@page "/logrecorddetails/{date:datetime}/{recordid:guid}"
@attribute [Authorize]

<ExerciseHistoryDetailsConnected RecordId="RecordId" Date="Date" />


@code {
		[Parameter]
		public Guid RecordId { get; set; }

		[Parameter]
		public DateTime Date { get; set; }
}