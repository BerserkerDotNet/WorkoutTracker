<RadzenButton Icon="timer" ButtonStyle="@(_isRunning ? ButtonStyle.Success : ButtonStyle.Primary)" Click="OnClick" Style="margin:15px 0 15px 0;width:calc(100% - 82px)">
	@(_isRunning ? ActiveText : IdleText)
</RadzenButton>